[project]
name = "vector-db"
version = "0.1.0"
description = ""
requires-python = ">=3.10"
dependencies = [
    "weaviate-client",
    "python-dotenv",
    "sentence-transformers",
    "datasets",
    "numpy",
    "tqdm",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

