[project]
name = "rag"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "weaviate-client",
    "sentence-transformers",
    "numpy",
    "pandas",
    "certifi",
    "datasets",
    "tqdm",
]

[tool.setuptools]
py-modules = []
